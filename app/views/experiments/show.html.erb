<!DOCTYPE html>
<html>
<head>
	 <meta charset="UTF-8">
     <meta name="description" content="about me">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title></title>
</head>
<style type="text/css">
	.videoWrapper {
	position: relative;
	padding-bottom: 56.25%; /* 16:9 */
	padding-top: 25px;
	height: 0;
	margin: 0 auto;
}
.videoWrapper iframe {
	position: absolute;
	top: 0;
	left: 0;
	
}
</style>
</head>
<body>
<h1><%= @experiment.title .html_safe %></h1>
<p><%= @experiment.description .html_safe %></p>
<p><%= @experiment.components %></p>
<p><%= @experiment.lab.title %></p>
<div class="videoWrapper">
<iframe width="853" height="480" src='https://www.youtube.com/embed/<%= @experiment.youtube_link .html_safe %>?rel=0&autoplay=<%= params[:autoplay] || 0 %>' frameborder='0' allowfullscreen></iframe>
</div>
<%= link_to "Back", root_path %>
<% if user_signed_in? %>
<% if @experiment.user_id == current_user.id %>
<%= link_to "Edit", edit_experiment_path(@experiment) %>
<%= link_to "Delete", experiment_path(@experiment), method: :delete, data: { confirm: "Are you sure?" } %>
<% end %>
<% end %>

</body>
</html>