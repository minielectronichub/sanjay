<%= form_tag search_path, method: :get do %>
  <div class="form-field">
    <%= text_field_tag :title,nil, 'search your experiment' %>
  </div>
  <div class="form-action">
    <%= submit_tag "Search" %>
  </div>
<% end %>